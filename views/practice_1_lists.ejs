<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <title>To Do List</title>
</head>

<body>

    <!-- <% if (kindOfDay==="Saturday" || kindOfDay==="Sunday" ) {%>
<% // we hv to end these tags in front of every line that is not html %>
        <h1 style="color: purple;">It's a <%= kindOfDay %> todo list</h1>
        <% } else { %>
            <h1 style="color: blue;">Its a <%= kindOfDay %> todo list</h1>
            <% } %> -->


            <h1> <%= kindOfDay %> </h1>
</body>

</html>